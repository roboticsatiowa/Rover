# CV Installation

Most installation information can be found [here](https://github.com/NVIDIA-ISAAC-ROS/isaac_ros_common.git).

# Launching Nvidia ROS Docker Container
Navigate to ``workspaces/isaac_ros-dev/src/isaac_ros_common/scripts`` and run ``run_dev.sh``

This will spawn the primary container instance, run this in another terminal instance to connect to the current container session in a new tty.

# Installing more modules
More modules can be found under [here](https://github.com/NVIDIA-ISAAC-ROS/isaac_ros_common.git). And and can be built and compiled to add more CV modules to the workspace.